package ezui;

/**
 * Defines how a component will be aligned within a `Container`.
 * The alignment can be one of the following: LEFT, CENTER, RIGHT, FILL.
 */
public enum Alignment {
    LEFT, CENTER, RIGHT, FILL
}
